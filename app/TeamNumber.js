'use client';

import styled from 'styled-components';
import { CascadeText } from '../components/CascadeText';

export const TeamNumber = styled(CascadeText)`
  font-family: var(--paladins-font);
  font-size: calc(12vw + 1.2rem);
  margin: 0;
  padding: 0;
  white-space: nowrap;
`;
